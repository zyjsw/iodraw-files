```mermaid
graph TD
    A[fa:fa-microchip 启动监控周期] --> B[fa:fa-database 扫描所有块擦除次数]
    B --> C{擦除次数阈值判断\n(动态/静态策略)}
    C -->|阈值内| D[fa:fa-chart-line 更新磨损统计表]
    C -->|超阈值| E[fa:fa-filter 筛选候选块池]
    E --> F[fa:fa-balance-scale 计算迁移成本]
    F --> G{迁移优先级判断}
    G -->|冷数据优先| H[fa:fa-snowflake 选取冷数据块]
    G -->|热数据优先| I[fa:fa-fire 选取热数据块]
    G -->|混合策略| J[fa:fa-adjust 动态权重分配]
    H/I/J --> K[fa:fa-copy 创建数据副本]
    K --> L[fa:fa-check-square 数据完整性校验]
    L --> M{校验结果}
    M -->|失败| N[fa:fa-exclamation-triangle 重试/报警]
    M -->|成功| O[fa:fa-hdd 写入目标块]
    O --> P[fa:fa-sync-alt 更新地址映射表]
    P --> Q[fa:fa-eraser 原块数据擦除]
    Q --> R[fa:fa-redo-alt 重置擦除计数器]
    R --> S[fa:fa-clipboard-list 维护元数据日志]
    S --> T[fa:fa-chart-bar 更新均衡统计]
    T --> U[fa:fa-bell 触发异常检测]
    U --> V{异常处理}
    V -->|坏块标记| W[fa:fa-ban 隔离故障块]
    V -->|均衡优化| X[fa:fa-tachometer-alt 调整迁移阈值]


```